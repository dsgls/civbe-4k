# Event/quest/etc dialogs

On the "resource pod searched" dialog (and probably other similar dialogs), the frame too small vertically. It covers the image and the "OK" button is inside, but the event text is below the frame with no background.

Probably the same issue: on the "Quest received" dialog, the dialog frame actually does cover both the image and the event text. But the objective (checkbox and "Find 2 Resource Pods (0 found)") is below the dialog and not inside it.
