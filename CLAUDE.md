# Project notes for agents

Read `DEVELOPMENT.md` first — layout, pipelines, and the engine facts live
there.

## Ghidra Analysis Projects

| Project | Purpose |
|---|---|
| `civbe` | `CivilizationBE_DX11.exe` as program `civbe_dx11` — ForgeUI engine internals (UI XML attribute parsing, control layout). `CheckBoxControl_*`/`ButtonControl_*`/`RadioButtonControl_*` functions are named. |
